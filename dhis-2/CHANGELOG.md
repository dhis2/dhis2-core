<!--
Author: Morten Svanæs
Created: 2026-06-17
AI Assisted
-->

# Changelog

A casual index of notable changes. Newest first.

- 2026-07-16 - **Started Spring 7 PR-MAJOR** Branch `spring7-pr-major`: Framework 7.0.8, Security 7.0.6, Authorization Server 7.0.6, Spring Data Redis 4.1.0, Session 4.1.0, JUnit 6.0.3; HttpHeaders non-Map, OAuth RestClient token client, AS package moves, Hibernate SpringSessionContext FQCN, temporary PKCE bridge. Plan: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/PLAN-SPRING-7-READINESS-MULTI-PR.md`.
- 2026-07-16 - **Spring 7 Phase 0 prep PRs all merged** A–G on master: #24457 jakarta, #24458 mechanical FW, #24459 spring-mobile, #24460 method security, #24461 AntPathRequestMatcher (+ #24462 lambda DSL via E), #24463 content negotiation. Next: PR-MAJOR version bump. Plan: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/PLAN-SPRING-7-READINESS-MULTI-PR.md`.
- 2026-07-16 - **Reviewed and fixed the Spring 7 readiness plan** All repo-facing claims verified correct against pom/master/PRs; fixed 3 internal inconsistencies (stale PKCE open-decision text vs the locked PKCE-by-default decision, matcher strategy re-listed as open, roadmap diagram showed C before E and a false linear chain). Review: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/REVIEW-PLAN-SPRING-7-READINESS.md`.
- 2026-07-16 - **Planned multi-PR Spring Framework/Security 7 readiness from Stian spike PR #24087** Focus on forward-compatible migrations on Spring 6.2/Security 6.5 (jakarta lifecycle, method security, lambda DSL, matchers, content negotiation) before the atomic FW7 bump. Plan: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/PLAN-SPRING-7-READINESS-MULTI-PR.md`.
- 2026-07-16 - **Opened Spring 7 prep PRs A/D/G** [#24457](https://github.com/dhis2/dhis2-core/pull/24457) jakarta lifecycle, [#24458](https://github.com/dhis2/dhis2-core/pull/24458) mechanical FW APIs, [#24459](https://github.com/dhis2/dhis2-core/pull/24459) drop spring-mobile-device. Plan: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/PLAN-SPRING-7-READINESS-MULTI-PR.md`.
- 2026-07-16 - **Applied Fable minor notes to Spring 7 plan** Session BOM alignment wording for PR-MAJOR; PR-D/F5 SMS-gateway SSRF collision note. Review: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/REVIEW-PLAN-SPRING-7-READINESS.md`.
- 2026-07-16 - **Opened Spring 7 prep PRs B/E** [#24460](https://github.com/dhis2/dhis2-core/pull/24460) method security + 2FA ctor + dead voter; [#24461](https://github.com/dhis2/dhis2-core/pull/24461) custom AntPathRequestMatcher + 40 unit tests. Next: PR-C lambda DSL.
- 2026-07-16 - **Opened Spring 7 prep PR-C** [#24462](https://github.com/dhis2/dhis2-core/pull/24462) Security filter-chain lambda DSL (stacked on #24461). Structural only; security review checklist in PR body. Next: PR-F content negotiation.
- 2026-07-16 - **Opened Spring 7 prep PR-F** [#24463](https://github.com/dhis2/dhis2-core/pull/24463) path-extension content negotiation without Spring 7 flags (literal-first handler mapping + SuffixMediaType strategy, 12 unit tests). Phase 0 prep stack complete; next is PR-MAJOR version bump after reviews.
- 2026-07-16 - **CI fixes on Spring 7 prep PRs** #24457 dependency-analysis ignore + NOSONAR pre-existing S4790/S2631; #24459 spotless blank line; #24461 Sonar S4144 test dedupe; #24463 LOOKUP_PATH clear + RequestPath re-parse for trailing-slash/suffix fallback (fixes 404 then BAD_REQUEST).
- 2026-07-15 - **Started minibox Uganda verification under `~/develop/DHIS2_EXPERIMENT_UGANDA`** SL DB + 10k DSE fixture, master WAR on :8281, app PR #577 zip installed, baseline metrics + OU parity report written. Verdict GO-WITH-CAVEATS. See minibox `CURRENT/decision/GO-NO-GO.md`.
- 2026-07-15 - **Deep-interviewed minibox verification plan for DHIS2-21757** Six components (setup, baseline, 10k synthetic fixture via metadata import, app #577, batch #24449, pragmatic pilot gate). Final ambiguity 16%. Spec: `.omc/specs/deep-interview-minibox-verify-21757.md` and `docs/PLAN-MINIBOX-VERIFY-DHIS2-21757.md`.
- 2026-07-14 - **Slideshow for DHIS2-21757 investigation** Self-contained 12-slide HTML deck covering symptoms, root cause, hypothesis stack, existing PRs, ranked options, validation plan, and ship rules. Open with any browser. File: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/SLIDES-DHIS2-21757-dataEntry-metadata.html`.
- 2026-07-14 - **Investigated Uganda `/api/dataEntry/metadata` failure (DHIS2-21757)** Root cause is lazy N+1 over the full data-writeable dataset graph (`DataSetElement` → `DataElement`/`CategoryCombo` + nested collections), with L2 `read-write` lock time as the wall-clock amplifier. Existing work: closed global batch PR #24380, open scoped batch PR #24449 (2.42), open Gatling harness PR #24384. Full analysis + validation plan: `/Users/netromsb/develop/dhis2/dhis2-core/dhis-2/docs/INVESTIGATION-DHIS2-21757-dataEntry-metadata.md`.
- 2026-07-03 - Implemented all findings from the PR #24355 quality review (commit `d29fe7c9637`, pushed to `DHIS2-21120_master`, PR description refreshed): moved the `forgotPassword`/`resetPassword`/`updateExpiredPassword` flows from `UserAccountController` into `UserAccountService`/`DefaultUserAccountService` (controller is now thin delegates with a single dependency), de-duplicated the shared `validateNewPassword`/`checkRecoveryLock` helpers, replaced the second bcrypt in the new==old check with plain string equality, added a bcrypt timing-equalization burn on the unknown-username path, folded `AuthUpdatePasswordPermitAllTest` into `AuthenticationControllerTest`, and added 6 unit tests for `updateExpiredPassword` (incl. the timing burn and guard order, only observable with mocks). Verified: 57+14 controller tests and 8 unit tests green; a 36-agent adversarial review confirmed only minor corners (guard-order message swap when password==username, policy-unreachable; `resetPassword` trim hardening for legacy whitespace usernames), both deliberately kept. See `REVIEW-DHIS2-21120-auth-updatePassword-quality.md` for the outcome log.
- 2026-07-03 - Ran a strict code-quality review of PR [#24355](https://github.com/dhis2/dhis2-core/pull/24355) (branch `DHIS2-21120_master`). Verdict: security design solid, but two structural items to fix before merge (move the `updatePassword` flow into `UserAccountService`; de-duplicate the validation block copy-pasted from `resetPassword`) plus a one-line bcrypt simplification. See `REVIEW-DHIS2-21120-auth-updatePassword-quality.md` for details.
- 2026-07-03 - Fixed the SonarCloud quality-gate blocker on PR #24355 (`java:S2699`, commit `3fa151973fd`, pushed): the lockout test now uses `assertEquals` on the response status instead of the `content(...)` helper Sonar does not recognise as an assertion. Also refreshed the PR #24355 description to the hardened final state.
- 2026-07-03 - Addressed the login-app review of PR #24355 (commit `586a7826de4`, pushed): reordered the guards so the current-password check runs before the expiry check (closes a username-enumeration channel via "Account is not expired"), added brute-force throttling by reusing the account-recovery lockout (gated on "block on failed logins"), and rejected new==old passwords. Added 3 tests + made the permitAll test order-independent. 58 tests pass. Review: `apps/login-app/docs/review-DHIS2-21120-PR24355-backend.md`. Finding 1 (2.42 additive backport) still pending confirmation.
- 2026-07-03 - Opened DRAFT PR [dhis2/dhis2-core#24355](https://github.com/dhis2/dhis2-core/pull/24355) (base `master`) for the DHIS2-21120 migration: new `POST /api/auth/updatePassword` + removal of legacy `account/password`. Committed as `25c3050d190`, pushed `DHIS2-21120_master`. Net diff +344/-158, 8 files, clean (re-enable churn net-cancelled).
- 2026-07-02 - Removed (not just deprecated) the legacy `POST /api/account/password` on `DHIS2-21120_master`, per decision: deleted `AccountController.updatePassword` + its now-unused `PasswordManager` field/import, reverted the `/**/account/password` `permitAll` line (back to master baseline), deleted `AccountPasswordPermitAllTest` and the orphaned dead `expired.js` (its only in-repo caller; no page referenced it), and dropped the `testUpdatePassword_NonExpired` case. The separate `GET /api/account/password` password-validation endpoint is untouched. The new `POST /api/auth/updatePassword` is the sole path. No in-repo caller remained.
- 2026-07-02 - Implemented the DHIS2-21120 Option-C migration on `DHIS2-21120_master`: new `UpdatePasswordRequest` DTO + `POST /api/auth/updatePassword` on `UserAccountController` (expired-only, no auto-login, fixes the legacy null-deref, no username enumeration), one `permitAll` line, `@Deprecated(forRemoval, since 2.44)` on `AccountController.updatePassword`, and tests (7 functional in `UserAccountControllerTest` + new `AuthUpdatePasswordPermitAllTest`). Kept the legacy `account/password` re-enable for BC. spotless clean, `dhis-web-api` build green, 56 tests pass (0 failures). Not committed. See `PLAN-DHIS2-21120-auth-updatePassword-migration.md`.
- 2026-07-02 - Planned the DHIS2-21120 migration: repurpose branch `DHIS2-21120_master` from re-enabling the legacy form-param `POST /api/account/password` to Option C — a new JSON `POST /api/auth/updatePassword` on `UserAccountController` (expired-only, no auto-login) + one `permitAll` line + `@Deprecated` the legacy method + web-api tests. Recommended defaults locked (keep legacy `permitAll` for BC, master-only, skip e2e) pending Morten's confirmation. See `PLAN-DHIS2-21120-auth-updatePassword-migration.md`.
- 2026-06-18 - Recorded the Jasper RCE **credit ordering** on advisory `GHSA-223c-29wj-9jrm` (description, so it survives to publication): **Baboucarr = first/original finder** (internal, Feb 2026), **Causal Security / `@cipher-creator` = second/independent finder** (Jun 2026). Mirrored in the F7 write-up. Formal GitHub credit field left to the security team at publication.
- 2026-06-18 - Reconciled the Jasper RCE duplicates (nothing closed): added a "Relates" link `DHIS2-21664` ↔ `DHIS2-20958`, and a comment on `DHIS2-20958` crediting **Baboucarr** as the original discoverer (Feb 2026) and recording the external report (Causal Security / `@cipher-creator`, Jun 2026 → `GHSA-223c-29wj-9jrm` + `DHIS2-21664`) plus the new low-priv CRUD-bypass detail. Internal discovery predates the external report — flagged for the CVE/credit decision.
- 2026-06-18 - Triaged Causal Security's 12-June follow-up batch (3 findings; their labels F6/F7/F8 → internal F8/F9/F10). **F8** UserRole privilege escalation — *partially confirmed* (the reporter's `[ALL]` self-grant is refuted by an existing guard; real escalation to arbitrary non-`ALL` authorities remains): `GHSA-358v-xm7h-p6h3` (High 8.8) + `DHIS2-21665`. **F9** DataSet custom-form stored XSS — confirmed: `GHSA-cv96-8mr5-q2m4` (High 8.7) + `DHIS2-21666`. **F10** SQL-View host file-read via PG file functions — confirmed validator gap, *deployment-conditional* (only on a superuser DB role; the dev `docker-compose.yml` ships one, production NOSUPERUSER is unaffected): `GHSA-rh7q-35g6-f7f5` (Medium 6.5) + `DHIS2-21667`. All Jiras Security Team only; Jira dedup clean. Caught a CVSS miscalc on F9 (reviewer said 8.3; correct is 8.7). See `security-reports/incoming-2026-06-18/F8|F9|F10-*.md`.
- 2026-06-18 - Triaged inbound **CRITICAL RCE** (F7) from Causal Security (Cipher / Shubham Raj, `@cipher-creator`): JasperReports `designContent` RCE — a low-priv `F_REPORT_*_ADD` user gets OS command execution via `POST /api/reports` + the ungated `GET /api/reports/{uid}/data` (JasperReports 6.20.0, no sandbox). Verified confirmed on master `87825e7` (byte-identical to pin). Draft advisory `GHSA-223c-29wj-9jrm` (CVSS 9.9) + Jira `DHIS2-21664` (Security Team only). Caught a severity miscalc — a reviewer said 9.1, but `PR:L`+`S:C` computes to 9.9 (GitHub-confirmed); the reporter's 9.9 was correct. Distinct from but shares the `designContent` authoring root with the triage XSS advisory `GHSA-8hqh-3hw8-whrj` (coordinate the fix). See `security-reports/incoming-2026-06-18/F7-jasperreports-designcontent-rce.md`.
- 2026-06-18 - Triaged inbound sec report F6 (FileResource BOLA/IDOR — enumerate + download other users' private MESSAGE_ATTACHMENT/DOCUMENT/JOB_DATA files; reporter Ray Sabee / raysabee). Verified confirmed on master `87825e7`. Created draft advisory `GHSA-qm95-cqhc-6pf3` (CVSS 6.5) + Jira `DHIS2-21661` (Security Team only). Overlaps the embargoed `GHSA-3hqm-gvxx-mg36` (audit-files IDOR, reporter M4xIq) — same root cause, different endpoint and different reporter; flagged for security-team CVE/credit reconciliation. See `security-reports/incoming-2026-06-18/F6-fileresource-bola-idor.md`.
- 2026-06-18 - Created tracking Jira `DHIS2-21662` for the embargoed `GHSA-3hqm-gvxx-mg36` (audit-files IDOR, reporter M4xIq) at Security Team only, and added a "Relates" link to F6 `DHIS2-21661` so the two same-root-cause issues are connected.
- 2026-06-17 - Created the remaining 4 security Jira issues after validation: F1 `DHIS2-21657`, F2 `DHIS2-21658`, F3 `DHIS2-21659`, F5 `DHIS2-21660` (F4 `DHIS2-21656` earlier). All 5 restricted to "Security Team only" (id 10002) — batch-verified 5/5 restricted, 0 public, 0 at wrong level. Each links its draft GHSA.
- 2026-06-17 - Created the F4 Jira issue `DHIS2-21656` as a validation test, restricted to Security Level "Security Team only" (id 10002) — verified applied (direct read + JQL `level` filter). Corrected `AGENTS.md` to the exact level name and added a verified acli how-to (the level "Security team only" does not exist; the real names are "Security Team only" / "Developers only").
- 2026-06-17 - Added a Jira security-level rule to `AGENTS.md` (new "Security: coordinated disclosure & Jira handling" section) — unpublished/embargoed vulns must never be filed at the public level.
- 2026-06-17 - Created 5 private draft GitHub Security Advisories for the inbound findings: F1 `GHSA-58gf-5cfp-wmhm`, F2 `GHSA-25j6-74pq-7vcv`, F3 `GHSA-8968-v478-gf9r`, F4 `GHSA-3p5f-3v95-rp9x`, F5 `GHSA-hx9p-89pf-3xm5`. All in `draft` state.
- 2026-06-17 - Triaged an inbound security email (5 findings vs DHIS2 2.43.0.1): wrote per-finding issue files and an advisory dedup cross-check. Independently re-verified all 5 against pinned commit `58a531e` and current master `87825e7` (all still present). F1/F2/F5 are new (no advisory); F3/F4 overlap `CVE-2022-41949` (F4 is an incomplete-fix continuation). See `security-reports/incoming-2026-06-17/README.md`.
