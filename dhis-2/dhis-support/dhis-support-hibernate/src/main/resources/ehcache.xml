<?xml version="1.0" encoding="UTF-8"?>
<!-- Add jsr107 namespace -->
<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
  xmlns='http://www.ehcache.org/v3'
  xmlns:jsr107='http://www.ehcache.org/v3/jsr107'
  xsi:schemaLocation="
            http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.10.xsd
            http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.10.xsd">

  <!-- Service definition to specify the default template for JSR-107 created caches.
       Statistics are enabled so per-region metrics can be exposed via the
       monitoring.ehcache.enabled Micrometer binding (see EhCacheMetricsConfig). -->
  <service>
    <jsr107:defaults default-template="defaultCacheTemplate" enable-statistics="true"/>
  </service>

  <!-- Store EVERY region by reference, restoring the pre-2.42 semantics.

       Regions not declared below are created at runtime through the JSR-107 API
       (hibernate-jcache MissingCacheStrategy.CREATE with an empty MutableConfiguration),
       and the JCache spec defaults that to store-BY-VALUE: Ehcache then serializes and
       deserializes every entry on every get and put (SerializingCopier), under READ_WRITE
       inside the region lock critical section. DHIS2 stored all regions by reference from
       2016 through 2.41 (Ehcache 2); the 2.42 Spring 6 upgrade flipped runtime-created
       regions to by-value as a silent side effect of that spec default.

       This CacheManager-level default copier overrides the JSR-107 by-value semantics for
       runtime-created caches (all typed Object/Object), so they store by reference like the
       declared caches. Safe for Hibernate's use: the cache holds Hibernate-internal
       disassembled state, never handed to application code, never mutated in place. Entries
       are consequently no longer required to be Serializable; a deployment overriding this
       file to add off-heap or disk tiers must configure serializers, exactly as before 2.42.
       Pinned by StoreByReferenceConfigTest. -->
  <default-copiers>
    <copier type="java.lang.Object">org.ehcache.impl.copy.IdentityCopier</copier>
  </default-copiers>

  <!-- Cache template definition (referenced by jsr107:defaults and potentially explicit caches) -->
  <!-- Based on old defaultCache settings -->
  <cache-template name="defaultCacheTemplate">
    <expiry>
      <!-- Time-to-live: 1 hour. This is the upper bound on how long a stale entry can survive
           (e.g. after an out-of-band database change, or a late cache-put re-inserting a value
           just evicted by a concurrent write under NONSTRICT_READ_WRITE). Entries reload once
           per hour, which is noise at production request rates. TTI (time-to-idle) is not set;
           only one of the two can be given here. -->
      <ttl unit="seconds">3600</ttl>
    </expiry>
    <resources>
      <!-- Max 1,000,000 entries on heap -->
      <heap unit="entries">1000000</heap>
    </resources>
  </cache-template>

  <!--   Hibernate Default Query Cache -->
  <cache alias="default-query-results-region" uses-template="defaultCacheTemplate">
    <!-- No overrides needed as it matches the old defaultCache settings -->
  </cache>

  <!-- Hibernate Default Entity Cache for Update Timestamps -->
  <cache alias="default-update-timestamps-region">
    <expiry>
      <none/> <!-- Timestamps should not expire -->
    </expiry>
    <resources>
      <!-- Max 5,000 entries on heap (from old config) -->
      <heap unit="entries">5000</heap>
    </resources>
  </cache>

  <!--
    The hottest Hibernate regions under concurrent load are DECLARED here as ehcache-native
    caches. Store-by-reference is already the CacheManager default (see default-copiers
    above); declaring a region does not change copy semantics. Explicit caches exist only
    to override heap size and expiry from defaultCacheTemplate.

    Regions created on demand through the JSR-107 API (hibernate-jcache
    MissingCacheStrategy.CREATE) still inherit the template bounds and the manager-wide
    IdentityCopier. Hibernate stores disassembled (immutable) cache entries, so
    by-reference is safe; DHIS2 2.41 ran Ehcache 2 by-reference for years.

    The region list is the measured hottest regions under concurrent tracker import and
    metadata read load; heap bounds are sized with headroom for large national deployments,
    not the demo database. Regions not declared here fall back to the jsr107 template above
    (bounded, store-by-reference). Per-region cache metrics (gets, hits, puts, evictions)
    are exposed via /api/metrics; a region evicting while hot is undersized and should be
    raised.
  -->

  <!-- Entity regions -->
  <cache alias="org.hisp.dhis.organisationunit.OrganisationUnit">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">200000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.CategoryOption">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.dataelement.DataElement">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.dataset.DataSet">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.dataset.DataSetElement">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">200000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.Category">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.CategoryOptionCombo">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">500000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.user.User">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.user.UserGroup">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">50000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.user.UserRole">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.program.Program">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">10000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.period.PeriodType">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">1000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.period.Period">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>
  <!-- Tracker-import hot regions: the Option region alone out-reads every other region by
       roughly two orders of magnitude under concurrent event imports against option-heavy
       programs. Declared here to pin a 200k heap bound; leaving it undeclared would still
       store by reference (default-copiers) but would use the 1M template heap. -->
  <cache alias="org.hisp.dhis.option.Option">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">200000</heap></resources>
  </cache>
  <!-- Entity-level cache restored after the OptionSet.hbm.xml -> JPA migration dropped it: the
       metadata query cache stores OptionSet ids only, so without this region every query-cache
       hit re-selects each OptionSet row by id (one select per option set in the result). -->
  <cache alias="org.hisp.dhis.option.OptionSet">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.trackedentity.TrackedEntityAttribute">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.trackedentity.TrackedEntityType">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">1000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.option.OptionSet.options">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>


  <!-- Collection regions (element ids only, entries are small) -->
  <cache alias="org.hisp.dhis.organisationunit.OrganisationUnit.children">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">200000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.CategoryCombo.categories">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.CategoryCombo.optionCombos">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.Category.categoryOptions">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.category.CategoryOptionCombo.categoryOptions">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">500000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.user.UserGroup.managedGroups">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">50000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.user.UserRole.restrictions">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.user.UserRole.authorities">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">20000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.dataelement.DataElement.dataSetElements">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.dataelement.DataElement.groups">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>
  <cache alias="org.hisp.dhis.dataelement.DataElement.legendSets">
    <expiry><ttl unit="seconds">3600</ttl></expiry>
    <resources><heap unit="entries">100000</heap></resources>
  </cache>

</config>
