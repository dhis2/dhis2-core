/**
 * TODO(ivo) document how field filtering works technically (parser => jackson writer/property
 * filter) and how Jackson is configured so we can actually stream. Also emphasize how its tested
 * including benchmark.
 */
package org.hisp.dhis.fieldfiltering.better;
