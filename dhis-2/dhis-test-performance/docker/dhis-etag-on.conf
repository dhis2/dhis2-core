connection.dialect = org.hibernate.dialect.PostgreSQLDialect
connection.driver_class = org.postgresql.Driver
connection.url = jdbc:postgresql://db/dhis
connection.username = dhis
connection.password = dhis

system.update_notifications_enabled = off

# ETag A/B side: ON (same as product default for single-node)
cache.api.etag.enabled = on

# Memory / metrics visibility for A/B and MEMORY-etag.md runs
monitoring.api.enabled = on
monitoring.jvm.enabled = on
monitoring.cache.etag.enabled = on
