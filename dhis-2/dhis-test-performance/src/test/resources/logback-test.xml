<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="org.hisp.dhis.test" level="info" />
    <!-- Uncomment to see TrackerTest debug logs -->
    <!-- <logger name="org.hisp.dhis.test.tracker.TrackerTest" level="debug" /> -->

    <root level="warn">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
