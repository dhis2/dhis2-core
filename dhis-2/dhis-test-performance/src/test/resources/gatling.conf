gatling {
  http {
    requestTimeout = 600000 # 10 minutes for long-running DHIS2 operations
  }
  data {
    leak {
      noActivityTimeout = 60 # Period, in seconds, for which Gatling may have no activity before considering a leak may be happening
    }
  }
}
