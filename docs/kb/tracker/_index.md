---
title: Tracker subsystem
type: concept
status: draft
last_verified:
  commit: 
  date: 
  by: 
sources: []
see_also:
  - authz/tracker-access.md
tags: [tracker]
confidence: low
open_questions:
  - Import pipeline (validation → preprocess → persist), identifier schemes, deduplication, relationship graph, program rule engine.
---

# Tracker subsystem

Placeholder. Document the `dhis-tracker` module: import pipeline, identifier schemes, deduplication, relationship model, program rules engine, event/enrollment state machine.

## Open questions

- Import pipeline (validation → preprocess → persist), identifier schemes, deduplication, relationship graph, program rule engine.
