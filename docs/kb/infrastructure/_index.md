---
title: Infrastructure
type: concept
status: draft
last_verified:
  commit: 
  date: 
  by: 
sources: []
see_also: []
tags: [infra, docker, deployment]
confidence: low
open_questions:
  - Docker images, `docker-compose.yml` / `docker-compose.doris.yml` topology, configuration sources (dhis.conf, env, system properties), DHIS2_HOME layout.
---

# Infrastructure

Placeholder. Document Docker images, compose topology, configuration resolution (`dhis.conf`, env vars, system properties), DHIS2_HOME layout, embedded-Tomcat profile.

## Open questions

- Docker images, `docker-compose.yml` / `docker-compose.doris.yml` topology, configuration sources (dhis.conf, env, system properties), DHIS2_HOME layout.
